[{"id":6765,"created_at":"2026-04-02T07:28:38.880045+00:00","repo_name":"GetyeTek/IDE","type":"rollback","data":{"sha":"6250da3e9810651b04d3f1aa9ade0263f72b5c06","message":"Rolled back to 6250da3"}},{"id":6780,"created_at":"2026-04-02T12:08:04.47223+00:00","repo_name":"GetyeTek/IDE","type":"patch","data":{"ops":[{"text":"Fix subsequent message pausing and implement one-time mobile video unlocking","action":"comment"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        async function sendMessage() {\n            // Unlock videos for mobile\n            vids.forEach(v => { v.play().then(() => v.pause()).catch(() => {}); });\n\n            const inputElement = document.getElementById('user-input');","replace_with":"        async function sendMessage() {\n            // Unlock videos for mobile only once\n            if (!window.vidsUnlocked) {\n                vids.forEach(v => { v.play().then(() => v.pause()).catch(() => {}); });\n                window.vidsUnlocked = true;\n            }\n\n            const inputElement = document.getElementById('user-input');"}],"sha":"f73349726e95dd6903cf684107950f7367c2082a","results":[{"file":"Rose/Rose.html","status":"updated","operations":[{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        async function sendMessage() {\n            // Unlock videos for mobile\n            vids.forEach(v => { v.play().then(() => v.pause()).catch(() => {}); });\n\n            const inputElement = document.getElementById('user-input');","replace_with":"        async function sendMessage() {\n            // Unlock videos for mobile only once\n            if (!window.vidsUnlocked) {\n                vids.forEach(v => { v.play().then(() => v.pause()).catch(() => {}); });\n                window.vidsUnlocked = true;\n            }\n\n            const inputElement = document.getElementById('user-input');"}]}],"success":true,"commit_sha":"f73349726e95dd6903cf684107950f7367c2082a","conduit_id":3480,"sha_before":"141f0a223340abfcd41da078e62a8ca5cc73fdf2"}},{"id":6781,"created_at":"2026-04-02T12:08:55.887994+00:00","repo_name":"GetyeTek/IDE","type":"patch","data":{"ops":[{"text":"Restore original seamless video flow and remove disruptive pausing logic","action":"comment"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        async function sendMessage() {\n            // Unlock videos for mobile only once\n            if (!window.vidsUnlocked) {\n                vids.forEach(v => { v.play().then(() => v.pause()).catch(() => {}); });\n                window.vidsUnlocked = true;\n            }\n\n            const inputElement = document.getElementById('user-input');","replace_with":"        async function sendMessage() {\n            const inputElement = document.getElementById('user-input');"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function playVid(index) {\n            vids.forEach(v => { v.pause(); v.style.opacity = '0'; });\n            currentVidIndex = index;\n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;\n            vids[index].play();\n\n            // Special logic for Video G (The Response)\n            if (index === 6) {\n                // The checkAndDisplay loop in sendMessage handles the trigger now\n            }\n        }","replace_with":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            currentVidIndex = index;\n            \n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;\n            \n            const playPromise = vids[index].play();\n            if (playPromise !== undefined) {\n                playPromise.then(() => {\n                    // Only hide the old video AFTER the new one starts playing to prevent black flicker\n                    if (oldIndex !== index) {\n                        vids[oldIndex].pause();\n                        vids[oldIndex].style.opacity = '0';\n                    }\n                }).catch(error => {\n                    console.log(\"Playback blocked, waiting for interaction\");\n                });\n            }\n        }"}],"sha":"756a1ca9f4fb3ef1cfffc021f1f81e62f94b01d9","results":[{"file":"Rose/Rose.html","status":"updated","operations":[{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        async function sendMessage() {\n            // Unlock videos for mobile only once\n            if (!window.vidsUnlocked) {\n                vids.forEach(v => { v.play().then(() => v.pause()).catch(() => {}); });\n                window.vidsUnlocked = true;\n            }\n\n            const inputElement = document.getElementById('user-input');","replace_with":"        async function sendMessage() {\n            const inputElement = document.getElementById('user-input');"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function playVid(index) {\n            vids.forEach(v => { v.pause(); v.style.opacity = '0'; });\n            currentVidIndex = index;\n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;\n            vids[index].play();\n\n            // Special logic for Video G (The Response)\n            if (index === 6) {\n                // The checkAndDisplay loop in sendMessage handles the trigger now\n            }\n        }","replace_with":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            currentVidIndex = index;\n            \n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;\n            \n            const playPromise = vids[index].play();\n            if (playPromise !== undefined) {\n                playPromise.then(() => {\n                    // Only hide the old video AFTER the new one starts playing to prevent black flicker\n                    if (oldIndex !== index) {\n                        vids[oldIndex].pause();\n                        vids[oldIndex].style.opacity = '0';\n                    }\n                }).catch(error => {\n                    console.log(\"Playback blocked, waiting for interaction\");\n                });\n            }\n        }"}]}],"success":true,"commit_sha":"756a1ca9f4fb3ef1cfffc021f1f81e62f94b01d9","conduit_id":3481,"sha_before":"f73349726e95dd6903cf684107950f7367c2082a"}},{"id":6782,"created_at":"2026-04-02T12:09:29.051221+00:00","repo_name":"GetyeTek/IDE","type":"patch","data":{"ops":[{"text":"Change API Key field to ordinary text to prevent password autofill","action":"comment"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"                <input type=\"password\" id=\"api-key\" placeholder=\"Paste your key here...\" \n                    class=\"w-full p-3 rounded-xl bg-white/10 border border-white/20 text-white focus:ring-2 focus:ring-purple-400 focus:outline-none transition-all\">","replace_with":"                <input type=\"text\" id=\"api-key\" placeholder=\"Paste your key here...\" \n                    autocomplete=\"off\" spellcheck=\"false\" autocorrect=\"off\"\n                    class=\"w-full p-3 rounded-xl bg-white/10 border border-white/20 text-white focus:ring-2 focus:ring-purple-400 focus:outline-none transition-all\">"}],"sha":"60cfa9e88a8dc25f97e77a6954f21ab87c4b8253","results":[{"file":"Rose/Rose.html","status":"updated","operations":[{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"                <input type=\"password\" id=\"api-key\" placeholder=\"Paste your key here...\" \n                    class=\"w-full p-3 rounded-xl bg-white/10 border border-white/20 text-white focus:ring-2 focus:ring-purple-400 focus:outline-none transition-all\">","replace_with":"                <input type=\"text\" id=\"api-key\" placeholder=\"Paste your key here...\" \n                    autocomplete=\"off\" spellcheck=\"false\" autocorrect=\"off\"\n                    class=\"w-full p-3 rounded-xl bg-white/10 border border-white/20 text-white focus:ring-2 focus:ring-purple-400 focus:outline-none transition-all\">"}]}],"success":true,"commit_sha":"60cfa9e88a8dc25f97e77a6954f21ab87c4b8253","conduit_id":3482,"sha_before":"756a1ca9f4fb3ef1cfffc021f1f81e62f94b01d9"}},{"id":6783,"created_at":"2026-04-02T12:14:29.607556+00:00","repo_name":"GetyeTek/IDE","type":"patch","data":{"ops":[{"text":"Fix video order persistence, robust filename matching, and sidebar save feedback","action":"comment"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function renderVideoConfig() {\n            const list = document.getElementById('video-config-list');\n            list.innerHTML = '';\n            videoConfig.sort((a,b) => a.order - b.order).forEach(conf => {\n                const div = document.createElement('div');\n                div.className = 'config-row flex items-center gap-2 bg-white/5 p-2 rounded-lg border border-white/10';\n                div.dataset.id = conf.id;\n                div.innerHTML = `\n                    <span class=\"text-white font-bold w-6 text-center\">${conf.id}</span>\n                    <input type=\"number\" class=\"order-input w-12 bg-black/20 border border-white/20 rounded px-1 text-xs text-white\" value=\"${conf.order}\">\n                    <label class=\"flex items-center gap-1 text-[10px] text-white/60\">\n                        <input type=\"checkbox\" class=\"loop-input\" ${conf.loop ? 'checked' : ''}> Loop\n                    </label>\n                    <button onclick=\"this.parentElement.remove()\" class=\"ml-auto text-red-400 hover:text-red-300 text-xs\">\n                        <i class=\"fa-solid fa-trash\"></i>\n                    </button>\n                `;\n                list.appendChild(div);\n            });\n        }","replace_with":"        function renderVideoConfig() {\n            const list = document.getElementById('video-config-list');\n            if (!list) return;\n            list.innerHTML = '';\n            // Sort a copy for display, but keep original data intact\n            const sorted = [...videoConfig].sort((a,b) => a.order - b.order);\n            sorted.forEach(conf => {\n                const div = document.createElement('div');\n                div.className = 'config-row flex items-center gap-2 bg-white/5 p-2 rounded-lg border border-white/10';\n                div.dataset.id = conf.id;\n                div.innerHTML = `\n                    <span class=\"text-white font-bold w-6 text-center\">${conf.id}</span>\n                    <input type=\"number\" class=\"order-input w-12 bg-black/20 border border-white/20 rounded px-1 text-xs text-white\" value=\"${conf.order}\">\n                    <label class=\"flex items-center gap-1 text-[10px] text-white/60 select-none\">\n                        <input type=\"checkbox\" class=\"loop-input\" ${conf.loop ? 'checked' : ''}> Loop\n                    </label>\n                    <button onclick=\"removeVideoConfig('${conf.id}')\" class=\"ml-auto text-red-400 hover:text-red-300 text-xs p-1\">\n                        <i class=\"fa-solid fa-trash\"></i>\n                    </button>\n                `;\n                list.appendChild(div);\n            });\n        }\n\n        function removeVideoConfig(id) {\n            videoConfig = videoConfig.filter(c => c.id !== id);\n            renderVideoConfig();\n        }"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function saveSettings() {\n            localStorage.setItem('aura_api_key', document.getElementById('api-key').value);\n            localStorage.setItem('aura_system_prompt', document.getElementById('system-prompt').value);\n            \n            const configRows = document.querySelectorAll('.config-row');\n            const newConfig = Array.from(configRows).map(row => ({\n                id: row.dataset.id,\n                order: parseInt(row.querySelector('.order-input').value),\n                loop: row.querySelector('.loop-input').checked\n            }));\n            \n            videoConfig = newConfig;\n            localStorage.setItem('aura_video_config', JSON.stringify(newConfig));\n            toggleSidebar();\n        }","replace_with":"        function saveSettings() {\n            const btn = event.target.closest('button');\n            const originalText = btn.innerText;\n\n            localStorage.setItem('aura_api_key', document.getElementById('api-key').value);\n            localStorage.setItem('aura_system_prompt', document.getElementById('system-prompt').value);\n            \n            const configRows = document.querySelectorAll('.config-row');\n            videoConfig = Array.from(configRows).map(row => ({\n                id: row.dataset.id,\n                order: parseInt(row.querySelector('.order-input').value || 0),\n                loop: row.querySelector('.loop-input').checked\n            }));\n            \n            localStorage.setItem('aura_video_config', JSON.stringify(videoConfig));\n            \n            // Visual Feedback\n            btn.innerText = \"Saved!\";\n            btn.classList.replace('bg-purple-600', 'bg-green-600');\n            \n            setTimeout(() => {\n                btn.innerText = originalText;\n                btn.classList.replace('bg-green-600', 'bg-purple-600');\n                toggleSidebar();\n                renderVideoConfig(); // Refresh UI to match sorted state\n            }, 800);\n        }"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function getVidIndexById(id) {\n            return vids.findIndex(v => {\n                const source = v.querySelector('source');\n                const src = source ? source.getAttribute('src') : '';\n                return src.includes(id + '.mp4');\n            });\n        }","replace_with":"        function getVidIndexById(id) {\n            return vids.findIndex(v => {\n                const source = v.querySelector('source');\n                if (!source) return false;\n                const src = source.getAttribute('src');\n                // Match 'A.mp4' regardless of path\n                return src === `${id}.mp4` || src.endsWith(`/${id}.mp4`);\n            });\n        }"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function handleVideoEnded(index) {\n            const config = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            if (config && config.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            const nextConfig = [...videoConfig].sort((a, b) => a.order - b.order)\n                .find(c => c.order > (config ? config.order : 0));\n\n            if (nextConfig) {\n                const nextIndex = getVidIndexById(nextConfig.id);\n                if (nextIndex !== -1) playVid(nextIndex);\n            } else {\n                playVid(index);\n            }\n        }","replace_with":"        function handleVideoEnded(index) {\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // If we have an AI response waiting, skip looping and try to find Video G (Response)\n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            // Standard loop logic\n            if (currentConf && currentConf.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            // Find next based on order\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentIndex = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentIndex + 1] || sorted[0]; // Loop back to start of sequence if at end\n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }"}],"sha":"3af3575ef3894a542023a89a854ec138e5eb0877","results":[{"file":"Rose/Rose.html","status":"updated","operations":[{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function renderVideoConfig() {\n            const list = document.getElementById('video-config-list');\n            list.innerHTML = '';\n            videoConfig.sort((a,b) => a.order - b.order).forEach(conf => {\n                const div = document.createElement('div');\n                div.className = 'config-row flex items-center gap-2 bg-white/5 p-2 rounded-lg border border-white/10';\n                div.dataset.id = conf.id;\n                div.innerHTML = `\n                    <span class=\"text-white font-bold w-6 text-center\">${conf.id}</span>\n                    <input type=\"number\" class=\"order-input w-12 bg-black/20 border border-white/20 rounded px-1 text-xs text-white\" value=\"${conf.order}\">\n                    <label class=\"flex items-center gap-1 text-[10px] text-white/60\">\n                        <input type=\"checkbox\" class=\"loop-input\" ${conf.loop ? 'checked' : ''}> Loop\n                    </label>\n                    <button onclick=\"this.parentElement.remove()\" class=\"ml-auto text-red-400 hover:text-red-300 text-xs\">\n                        <i class=\"fa-solid fa-trash\"></i>\n                    </button>\n                `;\n                list.appendChild(div);\n            });\n        }","replace_with":"        function renderVideoConfig() {\n            const list = document.getElementById('video-config-list');\n            if (!list) return;\n            list.innerHTML = '';\n            // Sort a copy for display, but keep original data intact\n            const sorted = [...videoConfig].sort((a,b) => a.order - b.order);\n            sorted.forEach(conf => {\n                const div = document.createElement('div');\n                div.className = 'config-row flex items-center gap-2 bg-white/5 p-2 rounded-lg border border-white/10';\n                div.dataset.id = conf.id;\n                div.innerHTML = `\n                    <span class=\"text-white font-bold w-6 text-center\">${conf.id}</span>\n                    <input type=\"number\" class=\"order-input w-12 bg-black/20 border border-white/20 rounded px-1 text-xs text-white\" value=\"${conf.order}\">\n                    <label class=\"flex items-center gap-1 text-[10px] text-white/60 select-none\">\n                        <input type=\"checkbox\" class=\"loop-input\" ${conf.loop ? 'checked' : ''}> Loop\n                    </label>\n                    <button onclick=\"removeVideoConfig('${conf.id}')\" class=\"ml-auto text-red-400 hover:text-red-300 text-xs p-1\">\n                        <i class=\"fa-solid fa-trash\"></i>\n                    </button>\n                `;\n                list.appendChild(div);\n            });\n        }\n\n        function removeVideoConfig(id) {\n            videoConfig = videoConfig.filter(c => c.id !== id);\n            renderVideoConfig();\n        }"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function saveSettings() {\n            localStorage.setItem('aura_api_key', document.getElementById('api-key').value);\n            localStorage.setItem('aura_system_prompt', document.getElementById('system-prompt').value);\n            \n            const configRows = document.querySelectorAll('.config-row');\n            const newConfig = Array.from(configRows).map(row => ({\n                id: row.dataset.id,\n                order: parseInt(row.querySelector('.order-input').value),\n                loop: row.querySelector('.loop-input').checked\n            }));\n            \n            videoConfig = newConfig;\n            localStorage.setItem('aura_video_config', JSON.stringify(newConfig));\n            toggleSidebar();\n        }","replace_with":"        function saveSettings() {\n            const btn = event.target.closest('button');\n            const originalText = btn.innerText;\n\n            localStorage.setItem('aura_api_key', document.getElementById('api-key').value);\n            localStorage.setItem('aura_system_prompt', document.getElementById('system-prompt').value);\n            \n            const configRows = document.querySelectorAll('.config-row');\n            videoConfig = Array.from(configRows).map(row => ({\n                id: row.dataset.id,\n                order: parseInt(row.querySelector('.order-input').value || 0),\n                loop: row.querySelector('.loop-input').checked\n            }));\n            \n            localStorage.setItem('aura_video_config', JSON.stringify(videoConfig));\n            \n            // Visual Feedback\n            btn.innerText = \"Saved!\";\n            btn.classList.replace('bg-purple-600', 'bg-green-600');\n            \n            setTimeout(() => {\n                btn.innerText = originalText;\n                btn.classList.replace('bg-green-600', 'bg-purple-600');\n                toggleSidebar();\n                renderVideoConfig(); // Refresh UI to match sorted state\n            }, 800);\n        }"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function getVidIndexById(id) {\n            return vids.findIndex(v => {\n                const source = v.querySelector('source');\n                const src = source ? source.getAttribute('src') : '';\n                return src.includes(id + '.mp4');\n            });\n        }","replace_with":"        function getVidIndexById(id) {\n            return vids.findIndex(v => {\n                const source = v.querySelector('source');\n                if (!source) return false;\n                const src = source.getAttribute('src');\n                // Match 'A.mp4' regardless of path\n                return src === `${id}.mp4` || src.endsWith(`/${id}.mp4`);\n            });\n        }"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function handleVideoEnded(index) {\n            const config = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            if (config && config.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            const nextConfig = [...videoConfig].sort((a, b) => a.order - b.order)\n                .find(c => c.order > (config ? config.order : 0));\n\n            if (nextConfig) {\n                const nextIndex = getVidIndexById(nextConfig.id);\n                if (nextIndex !== -1) playVid(nextIndex);\n            } else {\n                playVid(index);\n            }\n        }","replace_with":"        function handleVideoEnded(index) {\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // If we have an AI response waiting, skip looping and try to find Video G (Response)\n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            // Standard loop logic\n            if (currentConf && currentConf.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            // Find next based on order\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentIndex = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentIndex + 1] || sorted[0]; // Loop back to start of sequence if at end\n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }"}]}],"success":true,"commit_sha":"3af3575ef3894a542023a89a854ec138e5eb0877","conduit_id":3483,"sha_before":"60cfa9e88a8dc25f97e77a6954f21ab87c4b8253"}},{"id":6784,"created_at":"2026-04-02T12:20:32.783652+00:00","repo_name":"GetyeTek/IDE","type":"patch","data":{"ops":[{"text":"Sync video playback logic with user-defined order and loop settings","action":"comment"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function handleVideoEnded(index) {\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // If we have an AI response waiting, skip looping and try to find Video G (Response)\n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            // Standard loop logic\n            if (currentConf && currentConf.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            // Find next based on order\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentIndex = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentIndex + 1] || sorted[0]; // Loop back to start of sequence if at end\n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }","replace_with":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // If we have an AI response waiting, jump to Video G (Response) regardless of order\n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            // If current video is set to loop, stay here unless AI is ready\n            if (currentConf && currentConf.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            // Find next based on the sorted 'order' property\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; \n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"            // Start Sequence if not started\n            if (isFirstMessage) {\n                firstMessageTime = Date.now();\n                const indexA = getVidIndexById('A');\n                if (indexA !== -1) {\n                    setTimeout(() => playVid(indexA), 2000);\n                }\n            }","replace_with":"            // Start Sequence based on the LOWEST order defined in settings\n            if (isFirstMessage) {\n                firstMessageTime = Date.now();\n                const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n                if (sorted.length > 0) {\n                    const firstVidIdx = getVidIndexById(sorted[0].id);\n                    if (firstVidIdx !== -1) {\n                        setTimeout(() => playVid(firstVidIdx), 2000);\n                    }\n                }\n            }"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"                } else {\n                    // Subsequent messages: Wait for Video G (Index 6) to reach 3s\n                    if (aiResponseQueue && currentVidIndex === 6 && vids[6].currentTime >= 3.0) {\n                        displayAIResponse(aiResponseQueue);\n                        aiResponseQueue = null;\n                    } else {\n                        setTimeout(checkAndDisplay, 100);\n                    }\n                }","replace_with":"                } else {\n                    // Subsequent messages: Wait for Video G to reach 3s (finding G dynamically)\n                    const gIndex = getVidIndexById('G');\n                    if (aiResponseQueue && currentVidIndex === gIndex && vids[gIndex].currentTime >= 3.0) {\n                        displayAIResponse(aiResponseQueue);\n                        aiResponseQueue = null;\n                    } else {\n                        setTimeout(checkAndDisplay, 100);\n                    }\n                }"}],"sha":"8d2aad429b0309fc659f1bdd95d55d88ced737fb","results":[{"file":"Rose/Rose.html","status":"updated","operations":[{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function handleVideoEnded(index) {\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // If we have an AI response waiting, skip looping and try to find Video G (Response)\n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            // Standard loop logic\n            if (currentConf && currentConf.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            // Find next based on order\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentIndex = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentIndex + 1] || sorted[0]; // Loop back to start of sequence if at end\n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }","replace_with":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // If we have an AI response waiting, jump to Video G (Response) regardless of order\n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            // If current video is set to loop, stay here unless AI is ready\n            if (currentConf && currentConf.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            // Find next based on the sorted 'order' property\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; \n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"            // Start Sequence if not started\n            if (isFirstMessage) {\n                firstMessageTime = Date.now();\n                const indexA = getVidIndexById('A');\n                if (indexA !== -1) {\n                    setTimeout(() => playVid(indexA), 2000);\n                }\n            }","replace_with":"            // Start Sequence based on the LOWEST order defined in settings\n            if (isFirstMessage) {\n                firstMessageTime = Date.now();\n                const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n                if (sorted.length > 0) {\n                    const firstVidIdx = getVidIndexById(sorted[0].id);\n                    if (firstVidIdx !== -1) {\n                        setTimeout(() => playVid(firstVidIdx), 2000);\n                    }\n                }\n            }"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"                } else {\n                    // Subsequent messages: Wait for Video G (Index 6) to reach 3s\n                    if (aiResponseQueue && currentVidIndex === 6 && vids[6].currentTime >= 3.0) {\n                        displayAIResponse(aiResponseQueue);\n                        aiResponseQueue = null;\n                    } else {\n                        setTimeout(checkAndDisplay, 100);\n                    }\n                }","replace_with":"                } else {\n                    // Subsequent messages: Wait for Video G to reach 3s (finding G dynamically)\n                    const gIndex = getVidIndexById('G');\n                    if (aiResponseQueue && currentVidIndex === gIndex && vids[gIndex].currentTime >= 3.0) {\n                        displayAIResponse(aiResponseQueue);\n                        aiResponseQueue = null;\n                    } else {\n                        setTimeout(checkAndDisplay, 100);\n                    }\n                }"}]}],"success":true,"commit_sha":"8d2aad429b0309fc659f1bdd95d55d88ced737fb","conduit_id":3484,"sha_before":"3af3575ef3894a542023a89a854ec138e5eb0877"}},{"id":6785,"created_at":"2026-04-02T12:23:35.877783+00:00","repo_name":"GetyeTek/IDE","type":"patch","data":{"ops":[{"text":"Override default CSS visibility and sync initial video state with order settings","action":"comment"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        .seq-vid {\n            opacity: 1;\n            transition: none; /* Keep the instant opacity swap smooth */\n        }","replace_with":"        .seq-vid {\n            opacity: 0;\n            transition: opacity 0.3s ease; /* Smooth fade for the initial load */\n        }"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function initVideoManager() {\n            vids.forEach((vid, i) => {\n                vid.style.zIndex = vids.length - i;\n                vid.addEventListener('ended', () => handleVideoEnded(i));\n                vid.addEventListener('timeupdate', function() {\n                    if (this.currentTime > 0.1) {\n                        vids.forEach((v, idx) => {\n                            if(idx !== currentVidIndex) v.style.opacity = '0';\n                        });\n                        vids[currentVidIndex].style.opacity = '1';\n                    }\n                });\n            });\n            renderVideoConfig();\n        }","replace_with":"        function initVideoManager() {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const firstId = sorted.length > 0 ? sorted[0].id : 'A';\n            const firstIndex = getVidIndexById(firstId);\n\n            vids.forEach((vid, i) => {\n                // Reset all to hidden\n                vid.style.opacity = '0';\n                vid.style.zIndex = '1';\n                \n                vid.addEventListener('ended', () => handleVideoEnded(i));\n                vid.addEventListener('timeupdate', function() {\n                    if (this.currentTime > 0.1) {\n                        vids.forEach((v, idx) => {\n                            if(idx !== currentVidIndex) v.style.opacity = '0';\n                        });\n                        vids[currentVidIndex].style.opacity = '1';\n                    }\n                });\n            });\n\n            // Set the correctly ordered first video as the visible background\n            if (firstIndex !== -1) {\n                currentVidIndex = firstIndex;\n                vids[firstIndex].style.opacity = '1';\n                vids[firstIndex].style.zIndex = '10';\n                vids[firstIndex].play().catch(() => console.log(\"Waiting for user interaction to play background...\"));\n            }\n            renderVideoConfig();\n        }"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            currentVidIndex = index;\n            \n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;","replace_with":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            currentVidIndex = index;\n            \n            vids[index].style.zIndex = '10';\n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;"}],"sha":"2492bc99ae8e4a4e37eaa149e4d5e03fdeca0d2e","results":[{"file":"Rose/Rose.html","status":"updated","operations":[{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        .seq-vid {\n            opacity: 1;\n            transition: none; /* Keep the instant opacity swap smooth */\n        }","replace_with":"        .seq-vid {\n            opacity: 0;\n            transition: opacity 0.3s ease; /* Smooth fade for the initial load */\n        }"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function initVideoManager() {\n            vids.forEach((vid, i) => {\n                vid.style.zIndex = vids.length - i;\n                vid.addEventListener('ended', () => handleVideoEnded(i));\n                vid.addEventListener('timeupdate', function() {\n                    if (this.currentTime > 0.1) {\n                        vids.forEach((v, idx) => {\n                            if(idx !== currentVidIndex) v.style.opacity = '0';\n                        });\n                        vids[currentVidIndex].style.opacity = '1';\n                    }\n                });\n            });\n            renderVideoConfig();\n        }","replace_with":"        function initVideoManager() {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const firstId = sorted.length > 0 ? sorted[0].id : 'A';\n            const firstIndex = getVidIndexById(firstId);\n\n            vids.forEach((vid, i) => {\n                // Reset all to hidden\n                vid.style.opacity = '0';\n                vid.style.zIndex = '1';\n                \n                vid.addEventListener('ended', () => handleVideoEnded(i));\n                vid.addEventListener('timeupdate', function() {\n                    if (this.currentTime > 0.1) {\n                        vids.forEach((v, idx) => {\n                            if(idx !== currentVidIndex) v.style.opacity = '0';\n                        });\n                        vids[currentVidIndex].style.opacity = '1';\n                    }\n                });\n            });\n\n            // Set the correctly ordered first video as the visible background\n            if (firstIndex !== -1) {\n                currentVidIndex = firstIndex;\n                vids[firstIndex].style.opacity = '1';\n                vids[firstIndex].style.zIndex = '10';\n                vids[firstIndex].play().catch(() => console.log(\"Waiting for user interaction to play background...\"));\n            }\n            renderVideoConfig();\n        }"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            currentVidIndex = index;\n            \n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;","replace_with":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            currentVidIndex = index;\n            \n            vids[index].style.zIndex = '10';\n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;"}]}],"success":true,"commit_sha":"2492bc99ae8e4a4e37eaa149e4d5e03fdeca0d2e","conduit_id":3485,"sha_before":"8d2aad429b0309fc659f1bdd95d55d88ced737fb"}},{"id":6786,"created_at":"2026-04-02T12:28:52.945141+00:00","repo_name":"GetyeTek/IDE","type":"patch","data":{"ops":[{"text":"Prevent initial video autoplay and keep AI background paused until first message","action":"comment"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"            // Set the correctly ordered first video as the visible background\n            if (firstIndex !== -1) {\n                currentVidIndex = firstIndex;\n                vids[firstIndex].style.opacity = '1';\n                vids[firstIndex].style.zIndex = '10';\n                vids[firstIndex].play().catch(() => console.log(\"Waiting for user interaction to play background...\"));\n            }\n            renderVideoConfig();","replace_with":"            // Set the correctly ordered first video as the visible background (Static until first message)\n            if (firstIndex !== -1) {\n                currentVidIndex = firstIndex;\n                vids[firstIndex].style.opacity = '1';\n                vids[firstIndex].style.zIndex = '10';\n                vids[firstIndex].currentTime = 0; \n                vids[firstIndex].pause(); // Force pause on reload\n            }\n            renderVideoConfig();"}],"sha":"6c6128478ffc6c6d060be445d8c4934ae6c1f00d","results":[{"file":"Rose/Rose.html","status":"updated","operations":[{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"            // Set the correctly ordered first video as the visible background\n            if (firstIndex !== -1) {\n                currentVidIndex = firstIndex;\n                vids[firstIndex].style.opacity = '1';\n                vids[firstIndex].style.zIndex = '10';\n                vids[firstIndex].play().catch(() => console.log(\"Waiting for user interaction to play background...\"));\n            }\n            renderVideoConfig();","replace_with":"            // Set the correctly ordered first video as the visible background (Static until first message)\n            if (firstIndex !== -1) {\n                currentVidIndex = firstIndex;\n                vids[firstIndex].style.opacity = '1';\n                vids[firstIndex].style.zIndex = '10';\n                vids[firstIndex].currentTime = 0; \n                vids[firstIndex].pause(); // Force pause on reload\n            }\n            renderVideoConfig();"}]}],"success":true,"commit_sha":"6c6128478ffc6c6d060be445d8c4934ae6c1f00d","conduit_id":3486,"sha_before":"2492bc99ae8e4a4e37eaa149e4d5e03fdeca0d2e"}},{"id":6787,"created_at":"2026-04-02T12:35:28.967376+00:00","repo_name":"GetyeTek/IDE","type":"patch","data":{"ops":[{"text":"Upgrade video looping logic with conditional triggers and loop counters","action":"comment"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        let currentVidIndex = 0;\n        let aiResponseQueue = null;\n        let activeTypingId = null;\n        let firstMessageTime = 0;\n        let isFirstMessage = true;","replace_with":"        let currentVidIndex = 0;\n        let aiResponseQueue = null;\n        let activeTypingId = null;\n        let firstMessageTime = 0;\n        let isFirstMessage = true;\n        let messageSentDuringVid = false;\n        let currentLoopCount = 0;"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            currentVidIndex = index;\n            \n            vids[index].style.zIndex = '10';\n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;","replace_with":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            if (oldIndex !== index) {\n                currentLoopCount = 0;\n                messageSentDuringVid = false;\n            } else {\n                currentLoopCount++;\n            }\n\n            currentVidIndex = index;\n            vids[index].style.zIndex = '10';\n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        async function sendMessage() {\n            const inputElement = document.getElementById('user-input');\n            const text = inputElement.value.trim();\n            if (!text) return;\n\n            const welcomeMsg = document.getElementById('welcome-msg');","replace_with":"        async function sendMessage() {\n            const inputElement = document.getElementById('user-input');\n            const text = inputElement.value.trim();\n            if (!text) return;\n\n            messageSentDuringVid = true;\n\n            const welcomeMsg = document.getElementById('welcome-msg');"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // If we have an AI response waiting, jump to Video G (Response) regardless of order\n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            // If current video is set to loop, stay here unless AI is ready\n            if (currentConf && currentConf.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            // Find next based on the sorted 'order' property\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; \n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }","replace_with":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            if (currentConf && currentConf.loop) {\n                let shouldLoop = false;\n                const cond = currentConf.condition || 'always';\n                const max = parseInt(currentConf.maxLoops) || 0;\n\n                if (cond === 'always') shouldLoop = true;\n                else if (cond === 'onMessage' && messageSentDuringVid) shouldLoop = true;\n                else if (cond === 'onNoMessage' && !messageSentDuringVid) shouldLoop = true;\n\n                // Check loop count limit (0 = infinite)\n                if (shouldLoop && (max === 0 || currentLoopCount < max)) {\n                    playVid(index);\n                    return;\n                }\n            }\n\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; \n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function saveSettings() {\n            const btn = event.target.closest('button');\n            const originalText = btn.innerText;\n\n            localStorage.setItem('aura_api_key', document.getElementById('api-key').value);\n            localStorage.setItem('aura_system_prompt', document.getElementById('system-prompt').value);\n            \n            const configRows = document.querySelectorAll('.config-row');\n            videoConfig = Array.from(configRows).map(row => ({\n                id: row.dataset.id,\n                order: parseInt(row.querySelector('.order-input').value || 0),\n                loop: row.querySelector('.loop-input').checked\n            }));","replace_with":"        function saveSettings() {\n            const btn = event.target.closest('button');\n            const originalText = btn.innerText;\n\n            localStorage.setItem('aura_api_key', document.getElementById('api-key').value);\n            localStorage.setItem('aura_system_prompt', document.getElementById('system-prompt').value);\n            \n            const configRows = document.querySelectorAll('.config-row');\n            videoConfig = Array.from(configRows).map(row => ({\n                id: row.dataset.id,\n                order: parseInt(row.querySelector('.order-input').value || 0),\n                loop: row.querySelector('.loop-input').checked,\n                condition: row.querySelector('.cond-input')?.value || 'always',\n                maxLoops: parseInt(row.querySelector('.max-input')?.value || 0)\n            }));"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function renderVideoConfig() {\n            const list = document.getElementById('video-config-list');\n            if (!list) return;\n            list.innerHTML = '';\n            // Sort a copy for display, but keep original data intact\n            const sorted = [...videoConfig].sort((a,b) => a.order - b.order);\n            sorted.forEach(conf => {\n                const div = document.createElement('div');\n                div.className = 'config-row flex items-center gap-2 bg-white/5 p-2 rounded-lg border border-white/10';\n                div.dataset.id = conf.id;\n                div.innerHTML = `\n                    <span class=\"text-white font-bold w-6 text-center\">${conf.id}</span>\n                    <input type=\"number\" class=\"order-input w-12 bg-black/20 border border-white/20 rounded px-1 text-xs text-white\" value=\"${conf.order}\">\n                    <label class=\"flex items-center gap-1 text-[10px] text-white/60 select-none\">\n                        <input type=\"checkbox\" class=\"loop-input\" ${conf.loop ? 'checked' : ''}> Loop\n                    </label>\n                    <button onclick=\"removeVideoConfig('${conf.id}')\" class=\"ml-auto text-red-400 hover:text-red-300 text-xs p-1\">\n                        <i class=\"fa-solid fa-trash\"></i>\n                    </button>\n                `;\n                list.appendChild(div);\n            });\n        }","replace_with":"        function renderVideoConfig() {\n            const list = document.getElementById('video-config-list');\n            if (!list) return;\n            list.innerHTML = '';\n            const sorted = [...videoConfig].sort((a,b) => a.order - b.order);\n            sorted.forEach(conf => {\n                const div = document.createElement('div');\n                div.className = 'config-row flex flex-col gap-2 bg-white/5 p-3 rounded-lg border border-white/10';\n                div.dataset.id = conf.id;\n                div.innerHTML = `\n                    <div class=\"flex items-center gap-2\">\n                        <span class=\"text-white font-bold w-6\">${conf.id}</span>\n                        <input type=\"number\" class=\"order-input w-10 bg-black/20 border border-white/20 rounded px-1 text-xs text-white\" value=\"${conf.order}\">\n                        <label class=\"flex items-center gap-1 text-[10px] text-white/60\">\n                            <input type=\"checkbox\" class=\"loop-input\" ${conf.loop ? 'checked' : ''} onchange=\"renderVideoConfig()\"> Loop\n                        </label>\n                        <button onclick=\"removeVideoConfig('${conf.id}')\" class=\"ml-auto text-red-400 text-xs p-1\"><i class=\"fa-solid fa-trash\"></i></button>\n                    </div>\n                    ${conf.loop ? `\n                    <div class=\"grid grid-cols-2 gap-2 mt-1 border-t border-white/5 pt-2\">\n                        <div class=\"flex flex-col gap-1\">\n                            <label class=\"text-[9px] text-white/40 uppercase\">Condition</label>\n                            <select class=\"cond-input bg-black/40 text-white text-[10px] rounded border border-white/10 p-1\">\n                                <option value=\"always\" ${conf.condition === 'always' ? 'selected' : ''}>Always</option>\n                                <option value=\"onMessage\" ${conf.condition === 'onMessage' ? 'selected' : ''}>On Message</option>\n                                <option value=\"onNoMessage\" ${conf.condition === 'onNoMessage' ? 'selected' : ''}>On No Message</option>\n                            </select>\n                        </div>\n                        <div class=\"flex flex-col gap-1\">\n                            <label class=\"text-[9px] text-white/40 uppercase\">Max Loops</label>\n                            <input type=\"number\" class=\"max-input bg-black/40 text-white text-[10px] rounded border border-white/10 p-1\" \n                                value=\"${conf.maxLoops || 0}\" placeholder=\"0 = infinite\">\n                        </div>\n                    </div>\n                    ` : ''}\n                `;\n                list.appendChild(div);\n            });\n        }"}],"sha":"ed726eab8f151c61d03d875af5d63619bf97629e","results":[{"file":"Rose/Rose.html","status":"updated","operations":[{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        let currentVidIndex = 0;\n        let aiResponseQueue = null;\n        let activeTypingId = null;\n        let firstMessageTime = 0;\n        let isFirstMessage = true;","replace_with":"        let currentVidIndex = 0;\n        let aiResponseQueue = null;\n        let activeTypingId = null;\n        let firstMessageTime = 0;\n        let isFirstMessage = true;\n        let messageSentDuringVid = false;\n        let currentLoopCount = 0;"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            currentVidIndex = index;\n            \n            vids[index].style.zIndex = '10';\n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;","replace_with":"        function playVid(index) {\n            if (index === -1) return;\n            \n            const oldIndex = currentVidIndex;\n            if (oldIndex !== index) {\n                currentLoopCount = 0;\n                messageSentDuringVid = false;\n            } else {\n                currentLoopCount++;\n            }\n\n            currentVidIndex = index;\n            vids[index].style.zIndex = '10';\n            vids[index].style.opacity = '1';\n            vids[index].currentTime = 0;"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        async function sendMessage() {\n            const inputElement = document.getElementById('user-input');\n            const text = inputElement.value.trim();\n            if (!text) return;\n\n            const welcomeMsg = document.getElementById('welcome-msg');","replace_with":"        async function sendMessage() {\n            const inputElement = document.getElementById('user-input');\n            const text = inputElement.value.trim();\n            if (!text) return;\n\n            messageSentDuringVid = true;\n\n            const welcomeMsg = document.getElementById('welcome-msg');"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // If we have an AI response waiting, jump to Video G (Response) regardless of order\n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            // If current video is set to loop, stay here unless AI is ready\n            if (currentConf && currentConf.loop && !aiResponseQueue) {\n                playVid(index);\n                return;\n            }\n\n            // Find next based on the sorted 'order' property\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; \n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }","replace_with":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            if (currentConf && currentConf.loop) {\n                let shouldLoop = false;\n                const cond = currentConf.condition || 'always';\n                const max = parseInt(currentConf.maxLoops) || 0;\n\n                if (cond === 'always') shouldLoop = true;\n                else if (cond === 'onMessage' && messageSentDuringVid) shouldLoop = true;\n                else if (cond === 'onNoMessage' && !messageSentDuringVid) shouldLoop = true;\n\n                // Check loop count limit (0 = infinite)\n                if (shouldLoop && (max === 0 || currentLoopCount < max)) {\n                    playVid(index);\n                    return;\n                }\n            }\n\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; \n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function saveSettings() {\n            const btn = event.target.closest('button');\n            const originalText = btn.innerText;\n\n            localStorage.setItem('aura_api_key', document.getElementById('api-key').value);\n            localStorage.setItem('aura_system_prompt', document.getElementById('system-prompt').value);\n            \n            const configRows = document.querySelectorAll('.config-row');\n            videoConfig = Array.from(configRows).map(row => ({\n                id: row.dataset.id,\n                order: parseInt(row.querySelector('.order-input').value || 0),\n                loop: row.querySelector('.loop-input').checked\n            }));","replace_with":"        function saveSettings() {\n            const btn = event.target.closest('button');\n            const originalText = btn.innerText;\n\n            localStorage.setItem('aura_api_key', document.getElementById('api-key').value);\n            localStorage.setItem('aura_system_prompt', document.getElementById('system-prompt').value);\n            \n            const configRows = document.querySelectorAll('.config-row');\n            videoConfig = Array.from(configRows).map(row => ({\n                id: row.dataset.id,\n                order: parseInt(row.querySelector('.order-input').value || 0),\n                loop: row.querySelector('.loop-input').checked,\n                condition: row.querySelector('.cond-input')?.value || 'always',\n                maxLoops: parseInt(row.querySelector('.max-input')?.value || 0)\n            }));"},{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function renderVideoConfig() {\n            const list = document.getElementById('video-config-list');\n            if (!list) return;\n            list.innerHTML = '';\n            // Sort a copy for display, but keep original data intact\n            const sorted = [...videoConfig].sort((a,b) => a.order - b.order);\n            sorted.forEach(conf => {\n                const div = document.createElement('div');\n                div.className = 'config-row flex items-center gap-2 bg-white/5 p-2 rounded-lg border border-white/10';\n                div.dataset.id = conf.id;\n                div.innerHTML = `\n                    <span class=\"text-white font-bold w-6 text-center\">${conf.id}</span>\n                    <input type=\"number\" class=\"order-input w-12 bg-black/20 border border-white/20 rounded px-1 text-xs text-white\" value=\"${conf.order}\">\n                    <label class=\"flex items-center gap-1 text-[10px] text-white/60 select-none\">\n                        <input type=\"checkbox\" class=\"loop-input\" ${conf.loop ? 'checked' : ''}> Loop\n                    </label>\n                    <button onclick=\"removeVideoConfig('${conf.id}')\" class=\"ml-auto text-red-400 hover:text-red-300 text-xs p-1\">\n                        <i class=\"fa-solid fa-trash\"></i>\n                    </button>\n                `;\n                list.appendChild(div);\n            });\n        }","replace_with":"        function renderVideoConfig() {\n            const list = document.getElementById('video-config-list');\n            if (!list) return;\n            list.innerHTML = '';\n            const sorted = [...videoConfig].sort((a,b) => a.order - b.order);\n            sorted.forEach(conf => {\n                const div = document.createElement('div');\n                div.className = 'config-row flex flex-col gap-2 bg-white/5 p-3 rounded-lg border border-white/10';\n                div.dataset.id = conf.id;\n                div.innerHTML = `\n                    <div class=\"flex items-center gap-2\">\n                        <span class=\"text-white font-bold w-6\">${conf.id}</span>\n                        <input type=\"number\" class=\"order-input w-10 bg-black/20 border border-white/20 rounded px-1 text-xs text-white\" value=\"${conf.order}\">\n                        <label class=\"flex items-center gap-1 text-[10px] text-white/60\">\n                            <input type=\"checkbox\" class=\"loop-input\" ${conf.loop ? 'checked' : ''} onchange=\"renderVideoConfig()\"> Loop\n                        </label>\n                        <button onclick=\"removeVideoConfig('${conf.id}')\" class=\"ml-auto text-red-400 text-xs p-1\"><i class=\"fa-solid fa-trash\"></i></button>\n                    </div>\n                    ${conf.loop ? `\n                    <div class=\"grid grid-cols-2 gap-2 mt-1 border-t border-white/5 pt-2\">\n                        <div class=\"flex flex-col gap-1\">\n                            <label class=\"text-[9px] text-white/40 uppercase\">Condition</label>\n                            <select class=\"cond-input bg-black/40 text-white text-[10px] rounded border border-white/10 p-1\">\n                                <option value=\"always\" ${conf.condition === 'always' ? 'selected' : ''}>Always</option>\n                                <option value=\"onMessage\" ${conf.condition === 'onMessage' ? 'selected' : ''}>On Message</option>\n                                <option value=\"onNoMessage\" ${conf.condition === 'onNoMessage' ? 'selected' : ''}>On No Message</option>\n                            </select>\n                        </div>\n                        <div class=\"flex flex-col gap-1\">\n                            <label class=\"text-[9px] text-white/40 uppercase\">Max Loops</label>\n                            <input type=\"number\" class=\"max-input bg-black/40 text-white text-[10px] rounded border border-white/10 p-1\" \n                                value=\"${conf.maxLoops || 0}\" placeholder=\"0 = infinite\">\n                        </div>\n                    </div>\n                    ` : ''}\n                `;\n                list.appendChild(div);\n            });\n        }"}]}],"success":true,"commit_sha":"ed726eab8f151c61d03d875af5d63619bf97629e","conduit_id":3487,"sha_before":"6c6128478ffc6c6d060be445d8c4934ae6c1f00d"}},{"id":6795,"created_at":"2026-04-02T12:37:20.518344+00:00","repo_name":"GetyeTek/IDE","type":"patch","data":{"ops":[{"text":"Remove hardcoded Video G priority and enforce strict user-defined sequence logic","action":"comment"},{"action":"replace_block","file_path":"Rose/Rose.html","find_block":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            if (currentConf && currentConf.loop) {\n                let shouldLoop = false;\n                const cond = currentConf.condition || 'always';\n                const max = parseInt(currentConf.maxLoops) || 0;\n\n                if (cond === 'always') shouldLoop = true;\n                else if (cond === 'onMessage' && messageSentDuringVid) shouldLoop = true;\n                else if (cond === 'onNoMessage' && !messageSentDuringVid) shouldLoop = true;\n\n                // Check loop count limit (0 = infinite)\n                if (shouldLoop && (max === 0 || currentLoopCount < max)) {\n                    playVid(index);\n                    return;\n                }\n            }\n\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; \n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }","replace_with":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // 1. Check if we should LOOP the current video based on settings\n            if (currentConf && currentConf.loop) {\n                let shouldLoop = false;\n                const cond = currentConf.condition || 'always';\n                const max = parseInt(currentConf.maxLoops) || 0;\n\n                if (cond === 'always') shouldLoop = true;\n                else if (cond === 'onMessage' && messageSentDuringVid) shouldLoop = true;\n                else if (cond === 'onNoMessage' && !messageSentDuringVid) shouldLoop = true;\n\n                // Only loop if we haven't hit the max count (0 = infinite)\n                if (shouldLoop && (max === 0 || currentLoopCount < max)) {\n                    playVid(index);\n                    return;\n                }\n            }\n\n            // 2. If not looping, move to the NEXT video in the sorted order\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; // Loop back to the start of the list\n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }"}],"sha":"b3f20a2bc4ce9722210733a503da34571c6df395","results":[{"file":"Rose/Rose.html","status":"updated","operations":[{"type":"replace_block","score":100,"action":"replace_block","message":"Exact match","success":true,"file_path":"Rose/Rose.html","find_block":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            if (aiResponseQueue) {\n                const gIndex = getVidIndexById('G');\n                if (gIndex !== -1 && index !== gIndex) {\n                    playVid(gIndex);\n                    return;\n                }\n            }\n\n            if (currentConf && currentConf.loop) {\n                let shouldLoop = false;\n                const cond = currentConf.condition || 'always';\n                const max = parseInt(currentConf.maxLoops) || 0;\n\n                if (cond === 'always') shouldLoop = true;\n                else if (cond === 'onMessage' && messageSentDuringVid) shouldLoop = true;\n                else if (cond === 'onNoMessage' && !messageSentDuringVid) shouldLoop = true;\n\n                // Check loop count limit (0 = infinite)\n                if (shouldLoop && (max === 0 || currentLoopCount < max)) {\n                    playVid(index);\n                    return;\n                }\n            }\n\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; \n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }","replace_with":"        function handleVideoEnded(index) {\n            const sorted = [...videoConfig].sort((a, b) => a.order - b.order);\n            const currentConf = videoConfig.find(c => getVidIndexById(c.id) === index);\n            \n            // 1. Check if we should LOOP the current video based on settings\n            if (currentConf && currentConf.loop) {\n                let shouldLoop = false;\n                const cond = currentConf.condition || 'always';\n                const max = parseInt(currentConf.maxLoops) || 0;\n\n                if (cond === 'always') shouldLoop = true;\n                else if (cond === 'onMessage' && messageSentDuringVid) shouldLoop = true;\n                else if (cond === 'onNoMessage' && !messageSentDuringVid) shouldLoop = true;\n\n                // Only loop if we haven't hit the max count (0 = infinite)\n                if (shouldLoop && (max === 0 || currentLoopCount < max)) {\n                    playVid(index);\n                    return;\n                }\n            }\n\n            // 2. If not looping, move to the NEXT video in the sorted order\n            const currentPos = sorted.findIndex(c => c.id === (currentConf ? currentConf.id : ''));\n            const nextConf = sorted[currentPos + 1] || sorted[0]; // Loop back to the start of the list\n\n            if (nextConf) {\n                const nextIdx = getVidIndexById(nextConf.id);\n                if (nextIdx !== -1) playVid(nextIdx);\n            }\n        }"}]}],"success":true,"commit_sha":"b3f20a2bc4ce9722210733a503da34571c6df395","conduit_id":3489,"sha_before":"ed726eab8f151c61d03d875af5d63619bf97629e"}}]